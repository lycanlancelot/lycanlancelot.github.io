{
  "name": "lance-fit-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "JD -> tailored-CV fit checker for lance-song.com. Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
