# Liangjun (Lance) Song Portfolio

GitHub Pages source for `lycanlancelot.github.io`.

This site is built with the GitHub Pages `jekyll-theme-hacker` theme and custom Sass in `assets/css/style.scss`.

## Local Preview

```bash
bundle exec jekyll serve
```

The primary content lives in `index.md`.
